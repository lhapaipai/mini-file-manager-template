<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {% block stylesheets %}
            {{ vite_entry_link_tags('app.js') }}
        {% endblock %}

        {% block javascripts %}
            {{ vite_entry_script_tags('app.js') }}
        {% endblock %}
    </head>
    <body>
        <nav>
            <a href="{{ path("manager") }}">File Manager</a>
            <a href="{{ path("post_edit") }}">File Picker with form</a>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
