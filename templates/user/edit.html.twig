{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<p>Thème :
  <a href="{{ path('user_edit', {id: user.id}) }}">Penta (défaut)</a>
  {% for customTheme in themes %}
  <a href="{{ path('user_edit', {id: user.id, theme: customTheme }) }}">{{ customTheme }}</a>
  {% endfor %}
</p>

{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn">
  save
</button>
{{ form_end(form) }}

<a href="{{ path('user_index') }}">back to list</a>

{% endblock %}

{% block stylesheets %}
{{ parent() }}
{% if theme != 'penta' %}
{{ vite_entry_link_tags(theme ~ 'Theme') }}
{% endif %}
{{ vite_entry_link_tags('formFilePicker') }}
{% endblock %}

{% block javascripts %}
{{ parent() }}
{% if theme != 'penta' %}
{{ vite_entry_script_tags(theme ~ 'Theme') }}
{% endif %}
{{ vite_entry_script_tags('formFilePicker') }}
{% endblock %}